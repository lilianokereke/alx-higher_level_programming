#!/usr/bin/python3
turple_a = ()
turple_b = ()
def add_tuple(tuple_a=(), tuple_b=()):
 #   a = turple_a[0]
  #  b = turble_a[1]
    if len(turple_a) < 2:
        if turple_a == ():
            turple_a + (0, 0,)
        elif turple_a == (a,):
            turple_a + (0,)
    if len(turple_a) > 2:
        a = turple[0]
        b = turple[1]
    print (turple_a)
    print (turple_b)

print(add_tuple((),(1,3)))

